// @ts-check

/**
 * @file lib/tcp/client.js
 */

const { createConnection, Socket } = require("node:net");
const { EventEmitter } = require("node:events");

// Empty implemntation for TCP Client in js

